@startuml

|Rider|
start
   :Login;
   -> credentials;

|Hagglehaul| 
   if (authenticated?) is (yes) then
      |Rider|
      :Open home page;
      :Create trip;
      -> trip details;
      |Mapbox|
      :Produce map data;
      |Rider|
      :Post trip;
      |Hagglehaul|
      :Create trip object, add to list;
      |Rider|
      :Monitor trip status;
      stop
      
   else (no)
      |Rider|
      :Receive error;
      stop

@enduml