@startuml

|Rider|
start
   :Prompt to login;


|Hagglehaul|
   :Login;
   -> credentials;

|Data Server|
   :Authenticate;

|Hagglehaul| 
   if (authenticated?) is (yes) then
      |Rider|
      :Open landing page;
      :Create trip;
      -> trip details;
      |Mapbox|
      :Produce map data;
      |Rider|
      :Post trip;
      |Hagglehaul|
      :Create trip object;
      |Data Server|
      :Add trip to trip list;
      |Rider|
      :Monitor trip status;
      stop
      
   else (no)
      |Rider|
      :Receive error;
      stop

@enduml